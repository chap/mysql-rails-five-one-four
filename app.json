{
  "addons": [
    "jawsdb:kitefin",
  ],
  "environments": {
    "test": {
      "addons": [
        {
          "plan": "jawsdb:kitefin"        },
        "heroku-redis:in-dyno"
      ],
      "scripts": {
        "test-setup": "(sleep 120 && env) & env",
        "test": "env && rake test"
      }
    }
  }
}
