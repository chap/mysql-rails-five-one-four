{
  "environments": {
    "test": {
      "addons": [
        "cleardb",
        "rediscloud",
        "heroku-redis"
      ],
      "scripts": {
        "test": "env && rake test"
      }
    }
  }
}
