{
  "environments": {
    "test": {
      "addons": [
        "cleardb",
        "rediscloud",
        "heroku-redis:in-dyno"
      ],
      "scripts": {
        "test-setup": "(sleep 120 && env) & env",
        "test": "env && rake test"
      }
    }
  }
}
