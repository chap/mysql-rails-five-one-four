{
  "environments": {
    "test": {
      "addons": [
        "cleardb",
        "rediscloud"
      ],
      "scripts": {
        "test-setup": "(sleep 30 && env) & env",
        "test": "env && rake test"
      }
    }
  }
}
