{
  "environments": {
    "test": {
      "addons": [
        "cleardb",
        "rediscloud"
      ],
      "scripts": {
        "test-setup": "(sleep 120 && env) & env",
        "test": "env && rake test"
      }
    }
  }
}
