{
  "environments": {
    "test": {
      "addons": [
        "cleardb",
        "heroku-redis:in-dyno"
      ],
      "scripts": {
        "test-setup": "(sleep 120 && env) & env",
        "test": "env && rake test"
      }
    }
  }
}
